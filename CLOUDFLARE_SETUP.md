# Cloudflare Pages 部署指南

## 🚀 快速開始

### 第一步：訪問 Cloudflare Pages

1. 訪問 **https://pages.cloudflare.com**
2. 點擊右上角的 **"Sign up"** 或 **"Log in"**
3. 選擇 **"Continue with GitHub"** 使用 GitHub 帳號登錄

### 第二步：創建新項目

1. 登錄後，點擊 **"Create a project"** 按鈕
2. 在 **"Connect to Git provider"** 頁面：
   - 選擇 **GitHub**
   - 授權 Cloudflare 訪問你的 GitHub 帳號
   - 選擇倉庫：**`alexhuang0915/FishBroCapital-PTBot-Online`**

### 第三步：配置構建設置

Cloudflare Pages 通常會自動檢測 Next.js 項目，但我們需要確認設置：

#### 構建設置（Build settings）

- **Framework preset**: `Next.js`（應該會自動檢測）
- **Build command**: 
  ```bash
  npm run build
  ```
- **Build output directory**: 
  ```
  .next
  ```
- **Root directory**: 
  ```
  /（留空，表示根目錄）
  ```

#### 環境變量（Environment variables）

目前不需要設置環境變量，因為項目使用預處理的 JSON 文件。

如果需要，可以在 **"Environment variables"** 區域添加變量。

### 第四步：部署

1. 確認所有設置後，點擊 **"Save and Deploy"**
2. 等待構建完成（約 2-3 分鐘）
3. 部署成功後，你會看到一個 `*.pages.dev` 的域名，例如：
   - `fishbro-ptbot-online.pages.dev`

### 第五步：自定義域名（可選）

1. 在項目設置中，點擊 **"Custom domains"**
2. 輸入你的域名
3. 按照指示添加 DNS 記錄

## ⚙️ 構建配置詳情

### Next.js 構建命令

```bash
npm install
npm run build
```

### 構建輸出

Cloudflare Pages 會自動處理 Next.js 的構建輸出。不需要指定輸出目錄，因為 Cloudflare 會自動識別 Next.js 項目。

## 📋 部署流程

1. **推送代碼到 GitHub**
   ```bash
   git push
   ```

2. **Cloudflare 自動構建**
   - 檢測到新的 push
   - 自動觸發構建
   - 部署到 `*.pages.dev` 域名

3. **預覽部署**
   - 每次 Pull Request 也會創建預覽部署
   - 可以在 PR 中查看預覽鏈接

## 🔧 構建優化

### 構建時間優化

在 `package.json` 中，構建命令已經優化：
```json
{
  "scripts": {
    "build": "next build"
  }
}
```

### 緩存優化

Cloudflare Pages 會自動緩存：
- `node_modules`
- Next.js 構建緩存

## 🌐 域名設置

### 默認域名

每個項目會自動獲得：
- `your-project-name.pages.dev`

### 自定義域名

1. 在項目設置 → **Custom domains**
2. 添加你的域名
3. 更新 DNS 記錄：
   - 類型：`CNAME`
   - 名稱：`@` 或子域名（如 `www`）
   - 目標：`your-project-name.pages.dev`

## 📊 監控和日誌

### 查看構建日誌

1. 在項目中點擊 **"Deployments"** 標籤
2. 選擇一次部署
3. 查看 **"Build logs"** 了解構建過程

### 查看運行時日誌

1. 在項目設置中查看 **"Analytics"**
2. 查看請求統計和性能指標

## ⚠️ 常見問題

### 構建失敗

如果構建失敗，檢查：
1. **構建日誌** - 查看具體錯誤信息
2. **Node.js 版本** - Cloudflare 默認使用 Node.js 18+
3. **依賴安裝** - 確保 `package.json` 正確

### API 路由不工作

Cloudflare Pages 支持 Next.js API 路由，但需要：
- 使用 Next.js 14+（已使用）
- 確保 `app/api/` 路由正確配置（已配置）

### 數據載入問題

如果數據無法載入：
1. 確認 `public/data/strategies.json` 已提交到 Git
2. 檢查文件大小（應約 800KB）
3. 查看瀏覽器控制台的錯誤信息

## 🔄 從 Netlify 遷移

### 停用 Netlify

1. 在 Netlify 項目設置中
2. 可以選擇停用自動部署或刪除項目
3. **建議**：先確保 Cloudflare Pages 正常工作後再停用

### 保留域名

如果之前在 Netlify 使用自定義域名：
1. 在 Netlify 中移除域名
2. 在 Cloudflare Pages 中添加域名
3. 更新 DNS 記錄（如果使用 Cloudflare DNS，會自動更新）

## 📝 相關文件

- `DEPLOYMENT.md` - 部署流程說明
- `DEPLOYMENT_OPTIONS.md` - 其他部署選項比較

## ✅ 部署檢查清單

- [ ] 創建 Cloudflare Pages 帳號
- [ ] 連接 GitHub 倉庫
- [ ] 配置構建設置
- [ ] 首次部署成功
- [ ] 測試網站功能
- [ ] 測試 API 路由 (`/api/strategies`)
- [ ] 確認數據載入正常
- [ ] 設置自定義域名（可選）
- [ ] 停用 Netlify（可選）

## 🎉 完成！

部署完成後，你的網站將運行在 Cloudflare Pages 上，享受：
- ✅ 完全免費
- ✅ 無限帶寬
- ✅ 全球 CDN
- ✅ 自動部署
- ✅ Next.js 完整支持

如有任何問題，請查看 Cloudflare Pages 文檔：
https://developers.cloudflare.com/pages/

