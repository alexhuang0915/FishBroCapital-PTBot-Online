# Cloudflare Pages CLI 部署指南

## ⚠️ 重要說明

對於 **Next.js 應用（包含 API 路由）**，Cloudflare Pages 的最佳部署方式是通過 **Git 集成自動構建**，而不是 CLI 直接部署。

原因是：
- Next.js 需要在服務器端構建
- API 路由需要 Cloudflare Pages 的特殊處理
- 自動構建可以確保所有依賴正確安裝

## 但是，如果你堅持用 CLI...

### 方法 1：使用 Git 集成（推薦）✅

這是最簡單可靠的方式：

1. 確保代碼已推送到 GitHub
2. 在 Cloudflare Pages 網頁界面連接 GitHub 倉庫
3. 設置構建配置（Framework: Next.js）
4. 點擊部署

這樣每次 `git push` 都會自動部署。

---

### 方法 2：通過 CLI 觸發 Git 部署

如果你想用 CLI 操作，可以：

1. **登入 Cloudflare**
   ```bash
   wrangler login
   ```

2. **使用 wrangler pages 命令**
   ```bash
   wrangler pages project list
   ```

3. **連接現有項目**
   ```bash
   wrangler pages project create fishbro-ptbot-online
   ```

但最終還是需要在網頁界面配置 Git 集成。

---

## 🎯 推薦流程

### 步驟 1：確保代碼已推送到 GitHub

```bash
git add .
git commit -m "Ready for Cloudflare Pages deployment"
git push
```

### 步驟 2：在 Cloudflare Pages 網頁界面

1. 訪問 https://pages.cloudflare.com
2. 點擊 "Create a project"
3. 連接 GitHub 倉庫
4. 設置：
   - Framework: `Next.js`
   - Build command: `npm run build`
   - Build output: （留空）
5. 點擊 "Save and Deploy"

### 步驟 3：之後每次更新

只需要：
```bash
git push
```

Cloudflare Pages 會自動構建和部署！

---

## 💡 為什麼 CLI 不適合 Next.js？

- Next.js 需要服務器端構建環境
- API 路由需要特殊處理
- 依賴安裝和構建過程複雜
- Git 集成可以自動處理所有這些

---

## 🔧 如果真的要用 CLI...

我們可以幫你設置，但建議還是用 Git 集成方式，因為：
- ✅ 更簡單
- ✅ 更可靠
- ✅ 自動化
- ✅ 適合 Next.js

要試試 CLI 方式嗎？還是直接用 Git 集成？

